services:
  - type: web
    name: cryptext-backend
    env: python
    plan: free
    root: crypText/backend
    buildCommand: pip install -r ../requirements.txt
    startCommand: gunicorn -k eventlet -w 1 app:app
